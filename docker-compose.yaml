services:
  django_gunicorn:
    build: ./
    volumes:
      - ./:/app
    ports:
      - "8001:8000"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:$(/sbin/ip route|awk '/default/ { print $3 }')"
